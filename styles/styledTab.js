import { makeStyles } from "@mui/styles";

export const StyledTab = makeStyles({
  box: {
    width: "900px",
    marginBottom: "20px",
  },
  appBar: {
    position: "static",
    backgroundColor: "#ffffff",
  },
  tabs: { color: "#fb6f92" },
});
